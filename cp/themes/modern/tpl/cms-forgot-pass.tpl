<!-- if(${current_language} == 'en') -->
Dear ${user_data.name} , your need to use this <a href="${active_code_link}">link</a><br/>
or go to <a href="${active_link}">here</a> and use this code:<b>${active_code}</b> <br/>
in order to complete forgoten password process.
<!-- elseif(${current_language} == 'bg') -->
Уважаеми ${user_data.name} , моля използвайте този <a href="${active_code_link}">линк</a><br/>
или отидете на <a href="${active_link}">here</a> и използвайте този код : <b>${active_code}</b> <br/>
за да завършите процеса при забравена парола и активиране на нова.
<!-- elseif(${current_language} == '.....') -->
<!-- end -->